return {
  "ejrichards/baredot.nvim",
  opts = {
    -- Change this path
    git_dir = "$HOME/dotfiles",
    git_work_tree = "$HOME",
    -- Filename pattern to find that will disable baredot
    disable_pattern = "%.git"
  }
}
